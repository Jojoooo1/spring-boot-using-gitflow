management:
  endpoint:
    health:
      enabled: true
    info:
      enabled: true

spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
      config:
      #   # ConfigMap default
        name: carros
        namespace: local
        sources:
            - name: carros
              namespace: local
            - name: config-test # Not working: issue opened
              namespace: local
