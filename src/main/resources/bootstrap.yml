
spring:
  application:
    name: carros
  profiles:
    active: '@spring.profile.active@'
  # jpa:
  #   properties:
  #     hibernate:
        # ddl-auto: none
        # show_sql=false
        # format_sql=false

