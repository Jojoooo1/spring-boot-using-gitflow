spring:

  liquibase:
      change-log: classpath:/db/db.changelog-master.xml
  
  # Using H2 database by default since no datasource is set
  jpa:
    show_sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true

# logging:
#   config: classpath:log4j2.xml