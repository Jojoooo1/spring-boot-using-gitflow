spring:

  liquibase:
      change-log: classpath:/db/db.changelog-master.xml
  
  jpa:
    show_sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  config: conf/log4j2-dev.xml